{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/agentic-art-research/agent-attempt-result.schema.json",
  "title": "Agent attempt result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "run_id",
    "attempt_id",
    "status",
    "summary",
    "effect_key",
    "outputs",
    "failure",
    "human_decision_request",
    "diagnostics"
  ],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "run_id": {"type": "string", "pattern": "^HR[0-9]{3,}$"},
    "attempt_id": {"type": "string", "pattern": "^AT[A-Za-z0-9_-]{3,}$"},
    "status": {"enum": ["SUCCEEDED", "FAILED", "HUMAN_REQUIRED"]},
    "summary": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
    "effect_key": {
      "type": ["string", "null"],
      "pattern": "^[A-Za-z0-9._:/-]+$"
    },
    "outputs": {
      "type": "array",
      "items": {"$ref": "#/$defs/output"}
    },
    "failure": {
      "anyOf": [
        {"type": "null"},
        {"$ref": "#/$defs/failure"}
      ]
    },
    "human_decision_request": {
      "anyOf": [
        {"type": "null"},
        {"$ref": "#/$defs/humanDecisionRequest"}
      ]
    },
    "diagnostics": {"$ref": "#/$defs/diagnostics"}
  },
  "allOf": [
    {
      "if": {"properties": {"status": {"const": "SUCCEEDED"}}, "required": ["status"]},
      "then": {
        "properties": {
          "effect_key": {"type": "string"},
          "failure": {"type": "null"},
          "human_decision_request": {"type": "null"}
        }
      }
    },
    {
      "if": {"properties": {"status": {"const": "FAILED"}}, "required": ["status"]},
      "then": {
        "properties": {
          "effect_key": {"type": "null"},
          "failure": {"$ref": "#/$defs/failure"},
          "human_decision_request": {"type": "null"}
        }
      }
    },
    {
      "if": {"properties": {"status": {"const": "HUMAN_REQUIRED"}}, "required": ["status"]},
      "then": {
        "properties": {
          "effect_key": {"type": "null"},
          "failure": {"type": "null"},
          "human_decision_request": {"$ref": "#/$defs/humanDecisionRequest"}
        }
      }
    }
  ],
  "$defs": {
    "relativePath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(?!/)(?![A-Za-z]:)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"
    },
    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "kind", "value"],
      "properties": {
        "id": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
        "kind": {"enum": ["ARTIFACT", "PATCH", "REPORT", "REFERENCE", "TEXT", "JSON"]},
        "path": {"$ref": "#/$defs/relativePath"},
        "value": {}
      }
    },
    "failure": {
      "type": "object",
      "additionalProperties": false,
      "required": ["class", "message"],
      "properties": {
        "class": {
          "enum": [
            "WORKER-COMMAND",
            "WORKER-TIMEOUT",
            "WORKER-EXIT",
            "WORKER-PROTOCOL",
            "WORKER-OUTPUT-LIMIT",
            "WORKER-SECRET-OUTPUT",
            "WORKER-CAPABILITY"
          ]
        },
        "message": {"$ref": "common.schema.json#/$defs/nonEmptyString"}
      }
    },
    "humanDecisionRequest": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id", "project_id", "task_id", "human_decision_category", "question", "facts", "options",
        "recommended_option", "impact", "default_safe_action", "source_refs"
      ],
      "properties": {
        "id": {"type": "string", "pattern": "^DR[0-9]{3,}$"},
        "project_id": {"$ref": "common.schema.json#/$defs/projectId"},
        "task_id": {"type": "string", "pattern": "^[A-Z][A-Z0-9_-]{2,}$"},
        "human_decision_category": {"$ref": "common.schema.json#/$defs/humanDecisionCategory"},
        "question": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
        "facts": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/$defs/humanFact"},
          "uniqueItems": true
        },
        "options": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "#/$defs/humanOption"},
          "uniqueItems": true
        },
        "recommended_option": {"type": ["string", "null"]},
        "impact": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
        "default_safe_action": {
          "enum": ["DEFER", "REJECT", "CANCEL", "KEEP_CURRENT_SCOPE", "COMPLETE_WITH_GAPS"]
        },
        "source_refs": {
          "type": "array",
          "minItems": 1,
          "items": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
          "uniqueItems": true
        }
      }
    },
    "humanFact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "statement"],
      "properties": {
        "id": {"type": "string", "pattern": "^F[0-9]{3,}$"},
        "statement": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
        "source_refs": {
          "type": "array",
          "items": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
          "uniqueItems": true
        }
      }
    },
    "humanOption": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "label", "consequence"],
      "properties": {
        "id": {"type": "string", "pattern": "^OPT-[A-Za-z0-9_-]+$"},
        "label": {"$ref": "common.schema.json#/$defs/nonEmptyString"},
        "consequence": {"$ref": "common.schema.json#/$defs/nonEmptyString"}
      }
    },
    "diagnostics": {
      "type": "object",
      "additionalProperties": false,
      "required": ["stderr", "exit_code", "signal", "timed_out"],
      "properties": {
        "stderr": {"type": "string", "maxLength": 16384},
        "exit_code": {"type": ["integer", "null"]},
        "signal": {"type": ["integer", "null"], "minimum": 1},
        "timed_out": {"type": "boolean"},
        "stream": {"enum": ["stdout", "stderr"]},
        "bytes": {"type": "integer", "minimum": 0}
      }
    }
  }
}
